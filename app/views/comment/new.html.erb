<%= form_tag url_for(action: 'create'), method: "post" do %>
<div id="container">
<form>
  <div class="row">
    <div class="col">
<div class="col">
        <%= label_tag 'Contenu' %> <br />
        <%= text_field_tag 'content', @comment.content, class: "form-control", placeholder: 'Commentaire', autocomplete: 'off'%>
    </div>
  </div>

        <br /> 


        <%= submit_tag "Confirmer", class: "btn btn-info" %>  <%= link_to "Revenir à l'accueil", root_path, :class => 'btn btn-secondary'%>
</div>
      <% end %>